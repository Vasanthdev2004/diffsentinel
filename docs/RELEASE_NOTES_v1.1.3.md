# DiffSentinel v1.1.3 Release Notes

## Fixes

- Natural-language shell replies now handle greetings like `hi`, `hello`, and `gm` naturally.
- No-report diagnostic guidance is reserved for project-state questions instead of every plain text input.
- Live chat now shows an animated status with rotating DiffSentinel phrases while the model is generating.
